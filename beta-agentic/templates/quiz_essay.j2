[TASK]
Generate EXACTLY 5 essay questions in JSON format.
Never generate more or fewer.

[OUTPUT_SCHEMA]
{
  "pertanyaan": [
    {
      "id": "e1",
      "soal": "[Kalimat pertanyaan essay]",
      "rubrik": "[Kriteria penilaian skor tinggi]",
      "stimulus": "[Narasi pengantar atau konteks masalah]",
      "image_path": "[Isi dengan path gambar dari KONTEKS GAMBAR jika relevan dengan soal ini, jika tidak biarkan null]",
      "penjelasan": "[Penjelasan jawaban benar]"
    }
  ]
}

{% if image_context %}
[KONTEKS GAMBAR (MULTIMODAL)]
{{ image_context }}

*** INSTRUKSI MUTLAK UNTUK GAMBAR ***
Teks referensi di atas memiliki penanda [Referensi File Gambar: nama_file.png] tepat di atas teks yang menjelaskan gambar tersebut.
Pilih satu path gambar yang paling relevan dengan soal yang kamu buat, dan isikan ke dalam field `image_path` pada JSON struktur.
JANGAN menggunakan sintaks markdown gambar `![img](path)` di dalam soal. Cukup isikan URL path aslinya di field `image_path`.
{% endif %}

[LEVEL_CONFIGURATION]
{{ level_config }}

[SUBJECT_CONFIGURATION]
{{ subject_config }}

[LEARNING_CONTEXT]
{{ rag_context }}

[ATP_ALIGNMENT]
{{ atp }}

Prioritize ATP objectives as curriculum constraints.

[QUESTION_DESIGN_RULES]
- Frame questions around real Indonesian scenarios.
- Avoid simple recall questions ("Sebutkan", "Apa definisi").
- Require students to explain "Mengapa" or "Bagaimana".
- Do not make the question overly convoluted.

[RUBRIC_REQUIREMENTS]
- Keep rubrics concise and actionable.
- Define exactly what constitutes a high-score answer.

[EXPLANATION_REQUIREMENTS]
- Keep explanation direct and focused on the key concepts.
- Use clear cause-effect logic.

[STRICT_OUTPUT_RULES]
- Return raw JSON only.
- No markdown code block.
- No additional commentary.
- Escape newline characters.
- Escape internal quotes.
